#!/bin/sh

# Prevent pushing directly to main branch
BRANCH=$(git rev-parse --abbrev-ref HEAD)
if [ "$BRANCH" = "main" ]; then
  echo "❌ You can't push directly to the main branch!"
  echo "Please create a pull request instead."
  exit 1
fi

# Run tests before push
yarn test